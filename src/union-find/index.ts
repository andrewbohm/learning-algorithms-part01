class UnionFind {
  objectSize: number;

  constructor(objSize: number) {
   this.objectSize = objSize;
  }

  union(p: number, q: number): void {

  }

  contected(p: number, q: number) {
    return false;
  }
}
